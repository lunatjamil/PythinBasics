# Comparison Operators

name = "Jamil"

if len(name) < 3:
    print("Name has to be at least 3 or more characters")
elif len(name) > 50:
    print("Name can be a maximum of 50 characters")
else:
    print("Name looks good")
